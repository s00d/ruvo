[package]
name = "bench_loaded"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "bench_loaded"
path = "src/main.rs"

[dependencies]
ruvo = { path = "../../../crates/ruvo", features = ["cors", "cookies", "session", "rate-limit"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
serde_json = "1"
