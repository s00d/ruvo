[package]
name = "fs_demo"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "fs_demo"
path = "src/main.rs"

[dependencies]
sova = { workspace = true, features = ["fs"] }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
serde_json = { workspace = true }
