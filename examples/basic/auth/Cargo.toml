[package]
name = "auth"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "auth"
path = "src/main.rs"

[dependencies]
ruvo = { path = "../../../crates/ruvo", features = ["session", "cookies"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
serde_json = "1"
