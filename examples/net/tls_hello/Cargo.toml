[package]
name = "tls_hello"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "tls_hello"
path = "src/main.rs"

[dependencies]
ruvo = { path = "../../../crates/ruvo", features = ["dev-tls", "cors"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
serde_json = "1"
