[package]
name = "ruvo-quic"
version = "0.4.0"
edition = "2021"
description = "QUIC datagrams BackgroundService helpers for Ruvo"
license = "MIT"
publish = false

[features]
dev-tls = ["ruvo-core/dev-tls"]

[dependencies]
ruvo-core = { workspace = true, features = ["tls"] }
tokio = { workspace = true, features = ["net", "sync", "rt", "macros", "time"] }
tracing = { workspace = true }

bytes = { workspace = true }
futures-util = { workspace = true, default-features = false, features = ["std"] }

quinn = { workspace = true, features = ["rustls"] }
rustls = { workspace = true, default-features = false, features = ["ring", "std", "tls12", "logging"] }
h3 = { workspace = true }
h3-quinn = { workspace = true }
http = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "net", "time", "io-util"] }
tempfile = { workspace = true }
rcgen = { workspace = true }
rustls-pemfile = { workspace = true }
ruvo-core = { workspace = true, features = ["testing"] }

