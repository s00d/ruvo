[package]
name = "ruvo-shield"
version = "0.4.0"
edition = "2021"
description = "Security headers middleware for Ruvo"
license = "MIT"
publish = false

[dependencies]
ruvo-core = { path = "../../crates/ruvo-core" }

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
http = "1"
