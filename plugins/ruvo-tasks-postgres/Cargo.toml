[package]
name = "ruvo-tasks-postgres"
version = "0.4.0"
edition = "2021"
description = "Postgres TaskStore with FOR UPDATE SKIP LOCKED"
license = "MIT"
publish = false

[dependencies]
bytes = "1"
chrono = { version = "0.4", default-features = false, features = ["std", "clock"] }
ruvo-db = { path = "../ruvo-db" }
ruvo-tasks-store = { path = "../ruvo-tasks-store" }
sqlx = { version = "0.9", default-features = false, features = ["postgres", "runtime-tokio", "chrono"] }
tokio = { version = "1", features = ["rt", "macros"] }
tracing = "0.1"
