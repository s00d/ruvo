[package]
name = "ruvo-rate-limit"
version = "0.4.0"
edition = "2021"
description = "In-memory rate limit plugin for Ruvo"
license = "MIT"
publish = false

[dependencies]
ruvo-core = { path = "../../crates/ruvo-core" }

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
http = "1"
