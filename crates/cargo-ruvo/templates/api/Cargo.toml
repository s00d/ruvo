[package]
name = "{{name}}"
version = "0.1.0"
edition = "2021"

[dependencies]
ruvo = { path = "../crates/ruvo", features = ["api"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
