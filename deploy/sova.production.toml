# Production-shaped defaults for container / release runs.
# Secrets: use process env / .env — do not commit real credentials.

[server]
max_body = "10mb"
request_timeout = "60s"
trust_proxy = true

[production.server]
request_timeout = "30s"

[production.devtools]
enabled = false
